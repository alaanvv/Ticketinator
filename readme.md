# Ticketinator

Uses OpenAI models to respond a specific range  
on a discord server.  

You can set it to respond in a whole category or  
just in specific channels via the config command.  

It has a small history for chats so it saves some  
tokens, but you can edit it through the settings too.
